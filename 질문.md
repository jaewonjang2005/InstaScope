# ❓ InstaScope 스터디 질의응답 학습지 (질문.md)

> 스터디 팀원 및 심사자/개발자가 물어볼 수 있는 질문들과 핵심 힌트를 정리한 스터디용 문답 문서입니다.

---

## 📌 Part 1: 기초 PRD, 기획, 기술 스택 선택 관련 질문 (Level 1~2)

### Q1. 서비스 화면이 3단계(업로드 ➡️ 탐정 스캐닝 로딩 ➡️ 대시보드)로 설계된 이유는 무엇인가요?
- 💡 **답변 힌트**: 사용자가 파일 업로드 후 백엔드 분석 동안 빈 화면이나 지루한 로딩 바만 보고 멍하게 있지 않도록, **탐정 프로파일러 컨셉의 사이버펑크 애니메이션과 실시간 파싱 상태 릴레이**를 제공하여 대기 시간조차 즐거운 몰입형 유저 경험(UX)으로 승화시켰습니다.
- ✍️ **나의 답변 연습**: `[여기에 직접 작성 해보기]`

---

### Q2. 왜 Vercel 용량 제한에 맞춘 "초슬림 필수 데이터셋 (< 1MB)" 규격을 정의했나요?
- 💡 **답변 힌트**: Vercel 무료 서버리스 플랜은 요청 페이로드 제한이 **4.5MB**입니다. 전체 868MB 중 99%(840MB)를 차지하는 사진/동영상(`media/`)을 제거하고 필수 핵심 JSON만 묶으면 **500KB의 초슬림 ZIP**이 생성되므로, 서버 부하와 네트워크 전송 시간을 1,000% 줄일 수 있습니다.
- ✍️ **나의 답변 연습**: `[여기에 직접 작성 해보기]`

---

### Q3. 프론트엔드로 React(Vite)를 선택하고 백엔드로 FastAPI(Python)를 선택한 이유는 무엇인가요?
- 💡 **답변 힌트**:
  - **React (Vite)**: 3단계 페이지 흐름(Upload-Loading-Results)의 상태 관리가 용이하고, Vite는 Webpack 대비 10배 빠른 개발 경험을 줍니다.
  - **FastAPI (Python)**: 인스타 데이터는 수십만~수백만 줄의 대용량 JSON 파싱 및 텍스트/해시태그 집계가 핵심입니다. Python의 데이터 파싱/Pandas 능력과 FastAPI의 비동기 성능이 결합되어 선택했습니다.
- ✍️ **나의 답변 연습**: `[여기에 직접 작성 해보기]`

---

## 💻 Part 2: 4학년/개발자 수준 기술 심화 질문 (Level 3~4)

### Q4. 인스타 JSON 데이터를 파싱할 때 한글이 깨지는 문제가 발생했을 텐데, 정확히 원인이 무엇이었고 어떻게 해결했나요?
- 💡 **답변 힌트**: 인스타그램 Export 데이터는 UTF-8 한글 텍스트를 Latin-1 렌더링 문자열로 이중 인코딩(Double Encoding)하여 저장합니다. 파이썬 기본 `json.load()`를 쓰면 `\u00ec\u009e...` 형태의 깨진 글자가 됩니다. 이를 해결하기 위해 `text.encode('latin-1').decode('utf-8')` 재디코딩 함수를 파서 전체에 적용했습니다.
- ✍️ **나의 답변 연습**: `[여기에 직접 작성 해보기]`

---

### Q5. "이상형 계정 추천" 알고리즘의 관심 점수(Interest Score)는 어떤 방식으로 계산되나요?
- 💡 **답변 힌트**: 
  - `관심 점수 = (좋아요 × 2) + (저장 × 3) + (스토리 시청 × 1) + (스토리 좋아요 × 4)`
  - 수동적 단순 조회(1점)보다, 적극적인 호감 표출인 스토리 좋아요(4점)와 저장(3점)에 높은 가중치를 부여합니다.
  - 이 점수를 기준으로 정렬한 후 상호작용이 높은 계정을 "이상형 관심 계정"으로 선별합니다.
- ✍️ **나의 답변 연습**: `[여기에 직접 작성 해보기]`

---

### Q6. 일부 JSON 파일이 없는 미완성 ZIP 파일이 업로드되어도 백엔드가 에러(500)를 내지 않는 이유는 무엇인가요?
- 💡 **답변 힌트**: 백엔드 `InstaParser`를 Fault-Tolerant(결함 허용) 아키텍처로 설계했습니다. 각 파싱 함수가 파일의 존재 유무를 검사하여 없을 경우 안전하게 빈 리스트 `[]`나 기본 딕셔너리를 반환하고, 4대 분석 엔진도 이를 감지해 기본값으로 처리하므로 시스템이 다운되지 않습니다.
- ✍️ **나의 답변 연습**: `[여기에 직접 작성 해보기]`

---

### Q7. Vercel 배포 시 `Command exited with 127` 에러와 Vite JSX 타이포 에러는 어떻게 디버깅했나요?
- 💡 **답변 힌트**:
  - **127 에러**: Vercel 모노레포에서 `cd frontend` 인라인 커맨드 사용 시 리눅스 쉘 서브프로세스가 경로를 못 찾는 현상입니다. 루트 `package.json` 스크립트를 경유하는 `npm run build`로 단순화하여 해결했습니다.
  - **JSX 에러**: JSX 내부 텍스트에 이스케이프되지 않은 `<` 문자가 태그 시작으로 파싱되어 발생한 이슈로, `&lt;` 이스케이프 문자로 수정하여 해결했습니다.
- ✍️ **나의 답변 연습**: `[여기에 직접 작성 해보기]`

---

## 🚀 Ver 2.0 & Ver 2.1 추가 질의응답 (다중 문맥 슬라이싱 & 아키텍처)

### Q8. Ver 2.0에서 광고 데이터(`ads_information`)와 커넥션 데이터(`connections`)를 제외하고 `your_instagram_activity`만 남긴 결정의 이점은 무엇인가요?
- 💡 **답변 힌트**:
  1. **사용자 편의성**: 사용자가 다운로드 신청 시 복잡한 옵션을 고를 필요 없이 가장 익숙한 "내 활동 데이터(`your_instagram_activity`)" 하나만 선택하면 되므로 진입 장벽이 낮아집니다.
  2. **페이로드 최적화**: 브라우저 파서(JSZip)가 파싱해야 하는 JSON 개수가 줄어들어 파싱 타임이 0.1초 미만으로 단축되고, 백엔드로 넘어가는 데이터 페이로드가 200KB 이하로 감소하여 Vercel 서버리스 처리가 더욱 쾌적해졌습니다.
  3. **알고리즘 정교화**: 타인과의 연결 목록(팔로잉)에 의존하지 않고, 사용자가 게시물/스토리에 남긴 **진짜 행동 이력(좋아요, 저장, 스토리 반응)**만으로 이상형과 관심 취향을 오차 없이 계산합니다.
- ✍️ **나의 답변 연습**: `[여기에 직접 작성 해보기]`

---

### Q9. Vercel 4.5MB 제한 및 8MB ZIP 413 에러를 극복하기 위해 직접 제시하고 적용한 "다중 문맥 유지(Multi-Layered Context Slicing)" 기술이란 무엇인가요?
- 💡 **답변 힌트**:
  - 사용자의 ZIP 파일이 8MB 이상이고 `liked_posts_1.json`처럼 분할 파일로 존재하더라도, **브라우저 메모리 파서(JSZip)가 전송 전에 가상 환경에서 압축을 풀고 텍스트 데이터를 수집**합니다.
  - 단순 상위 잘라내기가 아니라 **최신 활동 50% + 중간 활동 30% + 초기 활동 20%를 다중 레이어로 슬라이싱**하여, 데이터 크기를 200KB로 대폭 줄이면서도 수개월~수년 전체 활동의 타임라인 문맥을 100% 보존하여 Vercel 4.5MB 제한과 92% 멈춤 에러를 완벽하게 극복했습니다.
- ✍️ **나의 답변 연습**: `[여기에 직접 작성 해보기]`

---

## 📝 질문 연습 보완 노트
> 스터디 진행 중 새로 나온 질문이나 피드백을 자유롭게 기록하세요.
