# ❓ InstaScope 스터디 질의응답 학습지 (질문.md)

> 스터디 팀원 및 심사자/개발자가 물어볼 수 있는 질문들과 핵심 힌트를 정리한 스터디용 문답 문서입니다.  
> 아래 힌트를 참고하여 본인만의 답변을 작성하고 연습해 보세요!

---

## 📌 Part 1: 기초 PRD, 기획, 기술 스택 선택 관련 질문 (Level 1~2)

### Q1. 인스타 데이터 다운로드 파일(ZIP)을 다루는 서비스를 기획한 계기가 무엇인가요?
- 💡 **답변 힌트**: 단순 통계(좋아요 수, 팔로워 수)는 인스타그램 앱 자체에서도 보여줍니다. 하지만 플랫폼이 나를 어떻게 규정하고 있는지(광고 카테고리), 내가 남들에게 보여주지 않는 비밀 취향(저장O, 좋아요X)은 앱에서 보여주지 않습니다. 이를 도파민 터지는 유저 경험으로 풀기 위해 기획했습니다.
- ✍️ **나의 답변 연습**: `[여기에 직접 작성 해보기]`

---

### Q2. 왜 프론트엔드로 React(Vite)를 선택하셨고 백엔드로 FastAPI(Python)를 선택하셨나요?
- 💡 **답변 힌트**:
  - **React (Vite)**: 대시보드 컴포넌트화, 차트 시각화(Chart.js), 빠른 렌더링에 최적화되어 있고, Vite는 기존 Webpack 대비 10배 이상 빠른 HMR 개발 경험을 줍니다.
  - **FastAPI (Python)**: 인스타그램 데이터는 수십만~수백만 줄의 대용량 JSON 파싱 및 텍스트/해시태그 집계가 핵심입니다. Python의 데이터 처리 능력(Pandas/Numpy)과 FastAPI의 비동기 속도가 결합되어 가장 적합했습니다.
- ✍️ **나의 답변 연습**: `[여기에 직접 작성 해보기]`

---

### Q3. 데이터베이스로 MongoDB 대신 Supabase(PostgreSQL)를 선택한 이유는 무엇인가요?
- 💡 **답변 힌트**:
  1. Postgres의 `JSONB` 타입을 사용하면 MongoDB처럼 스키마리스 JSON 저장이 완벽히 가능합니다.
  2. Supabase는 Vercel 배포 환경과 파트너십으로 클릭 한 번에 환경변수 및 보안 연결이 통합됩니다.
  3. RLS(Row Level Security) 정책을 통해 사용자 간 데이터 격리 보안을 간편하게 적용할 수 있습니다.
- ✍️ **나의 답변 연습**: `[여기에 직접 작성 해보기]`

---

## 💻 Part 2: 4학년/개발자 수준 기술 심화 질문 (Level 3~4)

### Q4. 인스타 JSON 데이터를 파싱할 때 한글이 깨지는 문제가 발생했을 텐데, 정확히 원인이 무엇이었고 어떻게 해결했나요?
- 💡 **답변 힌트**: 인스타그램 Export 데이터는 UTF-8 한글 텍스트를 Latin-1 렌더링 문자열로 이중 인코딩(Double Encoding)하여 저장합니다. 파이썬 기본 `json.load()`를 쓰면 `\u00ec\u009e...` 형태의 깨진 글자가 됩니다. 이를 해결하기 위해 `text.encode('latin-1').decode('utf-8')` 재디코딩 함수를 제작하여 파서 레이어 전체에 적용했습니다.
- ✍️ **나의 답변 연습**: `[여기에 직접 작성 해보기]`

---

### Q5. "이상형 계정 추천" 알고리즘의 관심 점수(Interest Score)는 어떤 방식으로 계산되나요?
- 💡 **답변 힌트**: 
  - `관심 점수 = (좋아요 × 2) + (저장 × 3) + (스토리 시청 × 1) + (스토리 좋아요 × 4)`
  - 수동적 단순 조회(1점)보다, 적극적인 호감 표출인 스토리 좋아요(4점)와 저장(3점)에 높은 가중치를 부여합니다.
  - 이 점수를 기준으로 계정들을 정렬한 후, 맞팔/친한친구가 아니면서 상호작용이 높은 계정을 "이상형 관심 계정"으로 선별합니다.
- ✍️ **나의 답변 연습**: `[여기에 직접 작성 해보기]`

---

### Q6. Vercel 서버리스 환경에서 대용량 ZIP 파일 업로드 시 발생할 수 있는 페이로드 제한(4.5MB)과 타임아웃 문제를 어떻게 해결했나요?
- 💡 **답변 힌트**: 
  1. 인스타 데이터 868MB 중 840MB(99%)는 분석에 불필요한 이미지/동영상(`media/`) 파일입니다.
  2. 서비스 안내 문구를 통해 사용자가 사진/동영상을 제외한 **가벼운 JSON 텍스트 전용 ZIP(3~5MB)**을 업로드하도록 유도했습니다.
  3. 백엔드 파서는 압축 해제 즉시 `media/` 폴더는 읽지 않고 무시하도록 유틸리티를 작성하여 처리 속도를 1초 이내로 단축시켰습니다.
- ✍️ **나의 답변 연습**: `[여기에 직접 작성 해보기]`

---

### Q7. 사용자의 개인 데이터가 서버에 남으면 프라이버시 문제가 생길 수 있는데, 보안과 데이터 휘발 처리는 어떻게 설계했나요?
- 💡 **답변 힌트**:
  1. **파일 시스템**: 서버에 업로드된 ZIP과 압축해제 폴더는 파싱 직후 파이썬 `tempfile` 스케줄러가 Disk에서 자동 삭제합니다.
  2. **DB 저장**: Supabase DB에는 개인 사진이나 이름 대신 익명화된 4대 분석 결과 JSONB 데이터만 `job_id` 기반으로 저장됩니다.
  3. **자동 소멸(TTL)**: DB 레코드는 1시간 후 소멸되도록 설계되었습니다.
- ✍️ **나의 답변 연습**: `[여기에 직접 작성 해보기]`

---

### Q8. Vercel 배포 시 `Command exited with 127` 에러와 Vite 8 Rolldown 이슈가 발생했었는데, 이를 어떻게 진단하고 해결했나요?
- 💡 **답변 힌트**:
  - **127 에러**: 리눅스 쉘에서 폴더나 명령어를 못 찾을 때 나는 에러입니다. `frontend` ➡️ `fe`로 폴더명을 바꾸면서 Vercel UI 설정의 기존 `cd frontend` 명령어와 충돌하여 발생했습니다. `package.json` 스크립트 기반 표준 `npm run build`로 정리하여 해결했습니다.
  - **Vite 이슈**: 윈도우 OneDrive 동기화 폴더 특성상 Vite의 `emptyOutDir` 폴더 삭제 동작이 파일 핸들 잠금을 일으켜 발생한 현상이었습니다. `vite.config.js`에 `emptyOutDir: false` 옵션을 부여하여 원천 해결했습니다.
- ✍️ **나의 답변 연습**: `[여기에 직접 작성 해보기]`

---

## 📝 질문 연습 보완 노트
> 스터디 진행 중 새로 나온 질문이나 피드백을 아래에 자유롭게 적고 보완하세요.
