# 🛠️ InstaScope 트러블슈팅 & 오류 해결 모음집 (오류.md)

> 개발 및 배포 과정에서 발생했던 주요 에러, 원인 분석, 해결 과정을 정리한 디버깅 기록 문서입니다.

---

## 📌 1. 인스타그램 JSON 한글 텍스트 이중 인코딩 깨짐
- **증상**: 파이썬 `json.load()`로 인스타 export JSON을 읽었을 때 한글이 `\u00ec\u009e\u00a5...` 형태로 심각하게 깨짐.
- **원인**: 인스타그램 데이터 다운로드 시스템이 UTF-8 한글 바이트를 Latin-1 문자열로 이중 인코딩(Double Encoding)하여 저장함.
- **해결**:
  ```python
  def decode_insta_text(text: str) -> str:
      try:
          return text.encode('latin-1').decode('utf-8')
      except (UnicodeDecodeError, UnicodeEncodeError):
          return text
  ```
  파서 전반에 재디코딩 유틸리티를 적용하여 정상 한국어로 복원.

---

## 📌 2. `parser.py` 함수 파라미터 괄호 문법 에러 (SyntaxError)
- **증상**: `SyntaxError: Function parameters cannot be parenthesized` 발생.
- **원인**: `def parse_liked_posts((self) -> List[Dict[str, Any]]:` 와 같이 self 주위에 이중 괄호 오타가 들어감.
- **해결**: `def parse_liked_posts(self) -> List[Dict[str, Any]]:` 로 괄호 수정.

---

## 📌 3. Windows 콘솔 `cp949` 이모지 출력 유니코드 오류 (UnicodeEncodeError)
- **증상**: 테스트 스크립트 실행 중 `UnicodeEncodeError: 'cp949' codec can't encode character '\U0001f525'` 발생.
- **원인**: 윈도우 PowerShell 표준 출력이 CP949(EUC-KR) 인코딩으로 설정되어 있어 불닭(🔥) 등 이모지를 터미널에 출력하지 못함.
- **해결**: 테스트 스크립트 상단에 `sys.stdout.reconfigure(encoding='utf-8')` 구문을 추가하여 UTF-8 강제 적용.

---

## 📌 4. `lucide-react` 존재하지 않는 아이콘 임포트 빌드 에러
- **증상**: `[MISSING_EXPORT] "Instagram" is not exported by "node_modules/lucide-react/dist/esm/lucide-react.mjs"` 에러로 Vite 빌드 중단.
- **원인**: `Header.jsx`에서 `lucide-react` 패키지에 존재하지 않는 `Instagram` 아이콘을 `import` 문에 포함시킴.
- **해결**: `Header.jsx`에서 미사용 아이콘 구문을 제거하고 `import { Sparkles, ShieldCheck } from 'lucide-react';` 로 정돈.

---

## 📌 5. Vite 8 알파/Rolldown 윈도우 경로 및 락파일 에러
- **증상**: `npm run build` 진행 시 1592개 모듈 변환 후 이렇다 할 로그 없이 Exit Code 1로 중단됨.
- **원인**: 최신 `create-vite`로 설치된 Vite 8(Rolldown 번들러) 알파 버전이 윈도우 유니코드 경로(`바탕 화면`)와 락파일 구조에서 불안정성을 보임.
- **해결**: `package.json`을 안정화 릴리즈 버전인 **Vite 5.4.11 + React 18.3.1** 로 패키지를 다운그레이드하고 `package-lock.json`을 클린 재설치.

---

## 📌 6. OneDrive 파일 핸들 잠금으로 인한 `vite build` 중단 에러 (`emptyOutDir`)
- **증상**: Vite 5 변경 후에도 `✓ 1592 modules transformed.` 직후 Exit Code 1 중단 현상 지속.
- **원인**: Windows OneDrive 동기화 폴더(`OneDrive/바탕 화면/...`) 특성상, Vite가 빌드 시작 전 기존 `dist/` 폴더를 지우려는 `emptyOutDir` 동작 과정에서 원드라이브 프로세스가 파일 핸들을 잠궈 권한 에러(EPERM)가 발생함.
- **해결**: `vite.config.js`에 `emptyOutDir: false` 옵션을 명시하여 기존 폴더 강제 삭제 동작을 건너뛰도록 설정.

---

## 📌 7. Vercel 배포 시 `Command "cd frontend && npm run build" exited with 127`
- **증상**: Vercel 배포 빌드가 시작되자마자 5초 만에 Exit Code 127로 실패.
- **원인**:
  1. `frontend` 폴더를 `fe`로 이름을 바꾸었다가 다시 `frontend`로 복원하는 과정에서 커밋 캐시가 꼬임.
  2. `vercel.json`에서 `"buildCommand": "cd frontend && npm run build"` 처럼 인라인 쉘 명령어를 쓰면 Vercel의 멀티파이프라인 서브쉘이 경로를 찾지 못함 (리눅스에서 Code 127 = No such file or directory).
- **해결**:
  1. 루트 `package.json`에 `"build": "cd frontend && npm install && npm run build"` 스크립트 작성.
  2. `vercel.json`의 빌드 명령어를 표준인 `"buildCommand": "npm run build"` 로 일원화하여 해결.

---

## 📌 8. Vercel Python 패키지 설치 경고 (`UV_LINK_MODE`)
- **증상**: Vercel 빌드 로그에 `warning: Failed to hardlink files; falling back to full copy.` 경고 출력.
- **원인**: Vercel의 파이썬 패키지 매니저(`uv`)가 가상 컨테이너 환경에서 하드링크 대신 전체 복사 방식으로 안전하게 패키지를 설치했다는 단순 안내성 경고문. (오류가 아니므로 빌드는 정상 진행됨)
- **해결**: 경고 출력을 지우기 위해 `vercel.json` 및 Vercel 환경변수에 `"UV_LINK_MODE": "copy"` 설정 추가.

---

## 📌 9. Vercel 서버리스 4.5MB 페이로드 및 10초 타임아웃 제한
- **증상**: 대용량 인스타 데이터 ZIP 업로드 시 웹사이트에서 "API 서버와의 통신에 실패했습니다" 네트워크 에러 발생.
- **원인**: Vercel Hobby(무료) 서버리스 파이썬 함수는 단일 요청 body 용량 제한이 **4.5MB**, 최대 실행시간 제한이 **10초**임.

---

## 📌 10. Vercel 4.5MB 페이로드 제한 극복 (JSZip 브라우저 실시간 파서 도입)
- **증상**: 9,988KB(~10MB) ZIP 업로드 시 Vercel 관문에서 413 Payload Too Large 에러 발생 및 Page 2(로딩) ➡️ Page 1(업로드) 화면 튕김 현상.
- **원인**: 10MB 이상의 무거운 ZIP 파일 전체를 네트워크로 보낼 때 Vercel 서버리스가 차단함.
- **해결**:
  1. 프론트엔드에 `JSZip` 브라우저 파서를 탑재하여 사용자의 ZIP 파일이 10MB이든 800MB이든 **브라우저 메모리 상에서 0.1초 만에 `your_instagram_activity` JSON 파일만 추출**.
  2. 가공된 **단 200KB~300KB 경량 JSON 페이로드**만 백엔드 `/api/upload-payload`로 전송하여 Vercel 용량 제한을 원천 무력화 및 속도 극대화.
