# 📌 인스타 알고리즘 분석 프로젝트 현황 및 향후 발전 방향 (2026.08.03)

## 1. 🏗️ 현재 프로젝트 구조 (Architecture)
현재 이 프로젝트는 인스타그램에서 추출한 사용자의 데이터를 분석하여, 겉으로 드러나는 '대중적 취향(SFW)'과 숨겨진 '은밀한 서브 취향(Secret/Spicy)'을 분리해내는 풀스택 웹 애플리케이션입니다.

* **Frontend (React + Vite)**: 
  * `SecretPickPage.jsx` 등 직관적이고 감각적인 UI를 제공합니다.
  * 애니메이션(Confetti) 및 감성적인 토글 스위치를 통해 사용자 경험(UX)을 극대화하고 있습니다.
* **Backend (Python FastAPI on Vercel)**:
  * 서버리스(Serverless) 환경에서 작동하며, 사용자가 업로드한 대용량 JSON 데이터를 청크(Chunk) 단위로 처리합니다.
  * `keyword_extractor.py`: 핵심 알고리즘 파일로, TF-IDF와 Sibling Penalty(동시 출현 태그 밀도 분석)를 통해 데이터를 수학적으로 필터링합니다.
  * `search_service.py`: 추출된 키워드를 기반으로 인스타그램 해시태그 탐색 링크(`/explore/tags/`)를 생성하여 새로운 콘텐츠 발견을 돕습니다.

---

## 2. 🎯 사용자가 원하는 방향성 (User's Core Direction)
사용자님(개발자)께서 추구하시는 핵심 철학은 **"하드코딩을 배제한 데이터 주도적이고, 세련되면서도 실용적인 서비스"**입니다.

* **수학적/데이터 기반 필터링**: "성공", "일상" 같은 단어를 수동으로 차단하지 않고, 알고리즘이 스스로 범용적인 노이즈 태그를 걸러내도록 요구하셨습니다. (TF-IDF 로직 도입 완료)
* **어뷰징 차단 (Sibling Penalty)**: 해시태그가 무분별하게 많이 달린 게시물은 '진짜 취향'이 아닌 조회수 목적의 스팸으로 간주하여 점수를 대폭 깎는 고도화된 접근을 원하셨습니다.
* **세련된 매운맛 (UI/UX)**: 자극적이기만 한 '불닭맛' 버튼을 없애고, '매운맛 ON/OFF 토글'처럼 애플(Apple) 감성의 깔끔하고 은밀한 UI로 19금/서브컬처 취향을 녹여내길 원하셨습니다.
* **새로운 트렌드 탐색**: 과거에 이미 본 게시물(URL)을 재탕하는 대신, 분석된 키워드를 바탕으로 인스타그램의 최신/인기 검색창으로 연결하여 '새로운 영감'을 얻기를 원하셨습니다.
* **유연한 결과 제공 (Quality > Quantity)**: 억지로 5개의 결과를 채우기 위해 노이즈 데이터를 끌어오지 않고, 확실한 데이터만 3~4개 선별하여 보여주는 정교함을 강조하셨습니다.

---

## 3. 🛠️ 앞으로 고쳐야 할 부분 (Future Improvements)
현재 시스템이 안정화되었으나, 더 완벽한 프로덕트를 위해 아래 내용들을 순차적으로 개선해야 합니다.

1. **사용되지 않는 잉여 변수 및 로직 완전 제거 (Memory Optimization)**:
   * 현재 백엔드(`routes.py`, `keyword_extractor.py`)에는 UI에서 삭제된 `buldak_tags`, `buldak_recs` 및 더 이상 사용하지 않는 `tag_to_urls` 로직이 여전히 계산되고 있습니다.
   * 이는 백엔드 처리 시간과 메모리를 낭비하므로(Vercel 타임아웃의 주 원인), 잉여 로직들을 깔끔하게 제거해야 합니다.
2. **동의어 및 형태소 분석 고도화 (Advanced NLP)**:
   * 현재는 "디지털노마드"와 "디지털 노마드"가 다른 태그로 인식될 수 있습니다. 띄어쓰기 제거나 가벼운 초성/형태소 분석을 적용하여 동의어를 병합(Merge)하는 로직이 필요합니다.
3. **대용량 파일 업로드 최적화 (Resilient Chunking)**:
   * 인스타그램 데이터가 수백 MB에 달할 경우, 프론트엔드에서 데이터를 쪼개어 보내는 과정에서 네트워크 지연이 발생할 수 있습니다. 업로드 진행률 바(Progress Bar)의 정확도를 높이고 재시도(Retry) 로직을 견고하게 짜야 합니다.

---

## 4. ⚠️ 주의 사항 (Precautions & Warnings)
개발 및 유지보수 과정에서 반드시 명심해야 할 크리티컬 리스크입니다.

* **Vercel Serverless 제한 (10초 타임아웃 & 250MB 용량)**:
   * 무거운 라이브러리(Pandas, Scipy, 대형 NLP 모델 등)를 추가하면 번들 사이즈 한도(250MB)를 초과하여 배포가 실패합니다. 
   * 또한 API 응답이 10초를 넘기면 강제로 타임아웃(500/504 에러)이 발생합니다. 데이터를 처리하는 알고리즘은 무조건 빠르고 가벼워야 합니다.
* **React State 삭제 시 잔여 코드 주의 (Black Screen 버그)**:
   * 상태 변수를 지울 때는, JSX 내부에서 해당 변수를 참조하는 삼항 연산자나 조건부 렌더링 로직도 반드시 함께 제거해야 합니다. 하나라도 남으면 런타임 에러로 인해 화면이 까맣게 변하는 버그가 발생합니다.
* **인스타그램 스크래핑 제재**:
   * 서버 측에서 직접 인스타그램 본문이나 이미지를 무단 크롤링하면 IP가 차단될 위험이 매우 큽니다. 현재처럼 사용자 데이터를 분석하고 결과를 인스타그램 공식 탐색 링크(`explore/tags`)로 넘겨주는 방식이 가장 안전합니다.

---

## 5. 💡 AI의 추가 제언 (Extra Insights & Recommendations)

* **A/B 테스트 및 파라미터 튜닝 옵션 제공**:
   * 현재 TF-IDF의 문서 빈도나 Sibling Penalty 임계값은 하드코딩된 휴리스틱 수치입니다. 추후 '분석 감도 조절' 슬라이더를 만들어, 사용자가 직접 "더 엄격하게 필터링하기" vs "다양하게 보기"를 선택할 수 있게 하면 훨씬 다이나믹한 결과물을 얻을 수 있습니다.
* **분석 결과 공유 기능 (Virality)**:
   * 사용자들이 자신의 '시크릿 픽' 결과를 인스타그램 스토리에 공유할 수 있도록, 예쁜 템플릿의 **결과 이미지 다운로드(캡처) 기능**을 추가하면 오가닉 바이럴(입소문)을 타기에 아주 좋습니다.
* **프라이버시(개인정보) 안심 문구 강조**:
   * 내밀한 취향을 다루기 때문에, 사용자는 데이터 유출에 대한 불안감을 가질 수 있습니다. 랜딩 페이지에 **"모든 분석은 기기와 일회성 메모리에서만 진행되며, 서버에 어떠한 개인 데이터도 영구 저장되지 않습니다."**라는 문구를 명확히 배치하여 신뢰감을 주는 것이 서비스 성공의 핵심입니다.
