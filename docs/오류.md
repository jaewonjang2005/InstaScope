# Insta Taste Recommender 개발 중 발생한 핵심 에러 및 해결 과정 (오류.md)

이 문서는 프로젝트를 개발하며 직면했던 치명적인 P0 버그 3가지와 그 극복 과정을 기록한 문서입니다.

---

## 💥 1. DuckDuckGo 검색 엔진 민감 쿼리 차단 (Scraping Block)
### 🚨 현상 (Symptom)
사용자의 인스타 상호작용 기록에서 일부 민감한 키워드가 빈번하게 도출됨. 이를 `duckduckgo-search`에 그대로 쿼리로 넣었으나, 검색 엔진의 필터링에 걸려 빈 리스트 `[]`만 반환되고 실질적인 콘텐츠 추천이 불가능해짐.

### 🔍 원인 분석 (Root Cause)
검색 API(DDGS) 내부 정책상 민감성 키워드를 API 호출로 검색할 시 봇으로 인지하거나 결과를 차단함.

### 🛠️ 해결 (Solution)
- 자체적인 **Safe Mapping Dictionary(안전 우회 사전)**를 백엔드에 구축했습니다.
- 특정 단어를 유저의 취향 단어를 건전하게 치환한 후 검색을 수행하여, 엔진 차단을 우회하고 유저의 의도에 맞는 콘텐츠를 안정적으로 긁어오도록 조치했습니다.

---

## 💥 2. 인스타그램 데이터 파싱 실패 (빈 데이터)
### 🚨 현상 (Symptom)
인스타그램 JSON에서 키워드와 해시태그를 추출하려 했으나 제대로 된 데이터가 추출되지 않음.

### 🔍 원인 분석 (Root Cause)
인스타그램 JSON 데이터의 깊이(depth)가 다르게 중첩되어 있었습니다. (예: `string_map_data` 내부 중첩)

### 🛠️ 해결 (Solution)
- 단순히 키값을 찾는 것이 아니라 내부 `dict`나 `list`가 있을 경우 재귀적(Recursive)으로 파고들어 탐색하는 안전한 구조로 변경하여 한 치의 오차 없이 태그를 추출해냈습니다.

---

## 💥 3. Vercel 500MB 임시 저장소(`/tmp`) 초과 에러
### 🚨 현상 (Symptom)
사용자가 800MB 크기의 압축 데이터를 업로드하면 서버가 500 에러를 뱉고 다운됨. Vector DB(pgvector)를 설치하려는 과정에서도 메모리 폭발.

### 🔍 원인 분석 (Root Cause)
Vercel Serverless Function은 실행 시 단 `500MB`의 쓰기 가능한 임시 디스크만 제공하며 거대한 DB 의존성을 버티지 못함.

### 🛠️ 해결 (Solution)
- **인메모리 스트리밍 파싱 (In-Memory Streaming Parsing)** 기법을 도입하여 디스크에 압축을 풀지 않고 단 4개의 JSON 파일만 RAM에 로드.
- Vector DB 구축을 전면 취소하고, **Real-time Web Scraping(DDGS)**을 활용해 그때그때 가벼운 상태로 웹에서 데이터를 검색해와 메모리 오버헤드를 제로(0)로 만들었습니다.
