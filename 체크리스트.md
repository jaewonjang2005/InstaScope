# 📋 InstaScope 최종 실행 & 검증 체크리스트 (체크리스트.md)

> 사용자가 인스타그램 데이터(ZIP)를 입력하고 실행했을 때, 웹사이트·Vercel·Supabase DB가 100% 정상 작동하는지 단계별로 점검하는 최종 검증 가이드입니다.

---

## 🖥️ 1. 웹사이트 3단계 UX 정상 작동 체크리스트 (Client UI)

- [ ] **[Page 1: 업로드 랜딩]**
  - [ ] 브라우저 탭 아이콘(Favicon)이 커스텀 네온 레이더 아이콘(`favicon.png`)으로 표시되는가?
  - [ ] `💡 4.5MB 이하 초슬림 ZIP 준비 가이드` 버튼 클릭 시 안내 팝업 모달이 정상적으로 뜨는가?
  - [ ] `.zip` 파일을 드롭존에 끌어다 놓거나 클릭하여 선택했을 때 업로드가 시작되는가?

- [ ] **[Page 2: 탐정 프로파일링 로딩]**
  - [ ] 업로드 시작 즉시 **탐정 스캐너 로딩 화면**으로 매끄럽게 전환되는가?
  - [ ] AI 생성 탐정 프로파일러 이미지(`detective.png`) 주변에 네온 펄스 스캐닝 애니메이션이 가동되는가?
  - [ ] 하단 프로그레스 바가 **0%에서 92%~100%까지 자연스럽게 상승**하는가?
  - [ ] 4단계 진단 메시지가 순차적으로 전환되는가?
    1. `🔍 알고리즘 레이더 조준 중...`
    2. `🔒 비밀 저장함 & 좋아요 교차 검증 중...`
    3. `💕 무의식 이상형 계정 딥 파싱 중...`
    4. `🕵️ Meta 광고 타겟팅 카테고리 폭로 중...`

- [ ] **[Page 3: 프로파일링 결과 대시보드]**
  - [ ] 분석 완료 시 화면에 **Confetti 축하 폭죽 애니메이션**이 팡 터지는가?
  - [ ] 결과 대시보드로 자동 슬라이드 전환되는가?
  - [ ] 4개 상단 탭(`🌶️ 취향 DNA`, `🔒 비밀 컬렉션`, `💕 이상형 계정`, `🕵️ 알고리즘 폭로`)을 클릭할 때마다 각 보고서 내용과 차트가 정상 표시되는가?
  - [ ] `🔄 새 분석하기` 버튼 클릭 시 다시 Page 1 업로드 화면으로 원상복구되는가?

---

## 🌐 2. Vercel 대시보드 검증 체크리스트 (Vercel Console)

Vercel 대시보드([vercel.com/dashboard](https://vercel.com/dashboard) ➡️ `InstaScope`) 접속 후 점검:

- [ ] **`Deployments` 탭 검증**:
  - [ ] 최상단 빌드 상태가 **🟢 `Ready`** (초록색)로 표시되는가?
  - [ ] `Source` 항목에 최신 GitHub 커밋 내용이 반영되어 있는가?
  - [ ] `Domains` 링크를 클릭했을 때 오류 페이지(404/500) 없이 서비스가 접속되는가?

- [ ] **`Logs` 탭 검증**:
  - [ ] 사용자가 웹에서 ZIP을 업로드할 때 실시간 로그에 `POST /api/upload` ➡️ **`200 OK`**가 찍히는가?
  - [ ] 결과 조회 시 `GET /api/results/{job_id}` ➡️ **`200 OK`**가 찍히는가?

---

## 🗄️ 3. Supabase DB 저장 검증 체크리스트 (Supabase Console)

Supabase 대시보드([supabase.com](https://supabase.com)) ➡️ `instascope-db` 접속 후 점검:

- [ ] **`Table Editor` 탭 검증 (★ 핵심!)**:
  - [ ] 좌측 표 모양 아이콘(`Table Editor`) ➡️ **`analysis_jobs`** 테이블 선택
  - [ ] 웹에서 분석이 완료될 때마다 **새로운 행(Row)**이 생성되는가?
  - [ ] 생성된 행의 `id`(UUID) 및 `created_at`(생성시각)이 정상 기록되는가?
  - [ ] `taste_dna`, `secret_collection`, `ideal_type`, `algorithm_expose` 컬럼에 JSON 데이터가 정상적으로 들어찼는가?

- [ ] **`SQL Editor` 탭 검증 (조회 테스트)**:
  - [ ] `SQL Editor` ➡️ 아래 쿼리를 입력하고 **Run**을 눌렀을 때 최근 5건의 분석 데이터가 조회되는가?

```sql
SELECT 
    id, 
    created_at, 
    taste_dna->>'spicy_level' AS spicy_level,
    taste_dna->>'spicy_score' AS spicy_score
FROM analysis_jobs 
ORDER BY created_at DESC 
LIMIT 5;
```

---

### 💡 모든 체크박스에 [x]가 표시되면 InstaScope 서비스가 100% 성공적으로 완벽하게 가동 중인 것입니다! 🎉
